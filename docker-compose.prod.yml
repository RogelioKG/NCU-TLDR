name: ncu-tldr-prod
services:
  db:
    image: postgres:18-alpine
    restart: unless-stopped

  frontend:
    build:
      dockerfile: Dockerfile
      target: prod
    restart: unless-stopped
    ports:
      - "80:8080"
    depends_on:
      - backend

  backend:
    build:
      dockerfile: Dockerfile
      target: prod
    restart: unless-stopped
