services:
  backend:
    build:
      target: production
    command: uv run uvicorn main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"

  frontend:
    build:
      target: production
    ports:
      - "80:80"
    depends_on:
      - backend
